{
  "name": "bug-report",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server/server.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "vite": "^7.1.7"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  }
}
